import { ClassNamePorValorPipe } from './class-name-por-valor.pipe';

describe('ClassNamePorValorPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassNamePorValorPipe();
    expect(pipe).toBeTruthy();
  });
});
